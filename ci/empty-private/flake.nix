{
  description = "Empty placeholder for CI builds without private repo access";

  inputs = {
    nixpkgs.url = "github:nixos/nixpkgs/nixos-unstable";
  };

  outputs = { self, nixpkgs, ... }: { };
}
